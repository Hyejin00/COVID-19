import produce from "immer";

const baseState = {
    
};

const reducer = produce((state, action) => {
    switch(action.type){
        default:
            break;
    }
}, baseState); 

export default reducer;